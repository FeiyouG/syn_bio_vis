// THIS Module contains css classes that used for styling components in simulation.js
import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({

  form: {
    display: 'flex',
    flexWrap: 'wrap',
    justifyContent: 'center',
  }

}));
